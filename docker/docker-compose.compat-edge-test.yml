services:
  python-rns:
    volumes:
      - ./scripts:/scripts:ro
    command: ["python3", "/scripts/python-compat-edge-test.py"]

  rust-node:
    volumes:
      - ./configs/rust-node/compat-edge-test.toml:/etc/reticulum/config.toml:ro
    environment:
      - RUST_LOG=debug
      - RUST_LOG_FORMAT=json
