services:
  python-rns:
    volumes:
      - ./scripts:/scripts:ro
    command: ["python3", "/scripts/python-keepalive-test.py"]

  rust-node:
    volumes:
      - ./configs/rust-node/keepalive-test.toml:/etc/reticulum/config.toml:ro
    environment:
      - RUST_LOG=debug
      - RUST_LOG_FORMAT=json
